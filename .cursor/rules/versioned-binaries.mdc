---
description: 编译程序用 vx.y.z 版本号，并与 git tag 一致
alwaysApply: true
---

# 编译版本号

本仓库编译输出为 `PortFwd-v<VERSION>.exe`，版本号见根目录 `VERSION`，格式 `x.y.z`。

- 小功能或 bug 修复：增加 z（`0.6.0` → `0.6.1`）
- 新功能或中等改动：增加 y，z 归零（`0.6.1` → `0.7.0`）
- 不要覆盖或删除旧的 `PortFwd-v*.exe`。历史版本由用户手动清理。
- 升版本：先改 `VERSION` → 提交 git 并打同号 tag（如 `v0.6.1`）→ 再编译。程序版本与 git tag 必须一致。
- Git 不会自动提交。exe 只留在磁盘，不要提交进 git。
- 已发布的两位版本（如 `v0.6`）保持原样。此后用三位：小改加 z，中等改动加 y。
